@page
@model SurveyWeb.Pages.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SurveyEntity[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SurveyEntity[0].Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SurveyEntity[0].Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.SurveyEntity) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                <input type="hidden" asp-for="@item.UserAnswers">
                <a asp-page="./Edit" asp-route-id="@item.SurveyId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.SurveyId">Fill Out</a> |
                <a asp-page="./Delete" asp-route-id="@item.SurveyId">Delete</a>
                
            </td>
        </tr>
}
    </tbody>
</table>
